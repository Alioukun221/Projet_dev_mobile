# TO_FIX

Suivi des corrections issues de l'audit global.

- [x] 1. Isoler les caches locaux par utilisateur et vider correctement au logout.
  - Fait: clés SharedPreferences et secure queue scopées par userId; scope activé dans `SupabaseDataService.init`; nettoyage au logout.
- [x] 2. Appliquer un consentement explicite a toute capture financiere, SMS inclus.
  - Fait: le master toggle/consentement bloque notifications et SMS; le texte de consentement mentionne les SMS.
- [x] 3. Rendre le handler SMS background compatible avec l'initialisation du cache.
  - Fait: handler background async avec initialisation plugins/cache et scope dernier utilisateur actif.
- [x] 4. Stabiliser les rappels todo: timezone et identifiants de notifications.
  - Fait: timezone explicite pour les rappels et identifiants de notification deterministes.
- [x] 5. Fiabiliser les budgets `spent` en offline et corriger le trigger Supabase.
  - Fait: recalcul local de `spent` depuis les transactions cachees et trigger SQL robuste sur OLD/NEW.
- [x] 6. Eviter les demandes de permission au demarrage sans contexte.
  - Fait: suppression de la demande storage au demarrage; permissions rappels demandees lors de la planification utilisateur, pas au splash.
- [x] 7. Corriger la saisie d'heure des transactions manuelles.
  - Fait: ajout d'un `showTimePicker` dans ajout/edition transaction et affichage date+heure.
- [x] 8. Ameliorer le traitement des erreurs utilisateur et service.
  - Fait: helper `userErrorMessage`, suppression de plusieurs `$e` bruts dans les formulaires principaux, feedback visible dans les reglages.
- [x] 9. Rendre la validation des transactions en attente fiable en cas d'echec.
  - Fait: `approvePending`/`approveAll` renvoient un resultat; le swipe ne supprime plus l'item en cas d'echec.
- [x] 10. Centraliser locale/devise au lieu de forcer `fr_FR`/`CFA`.
  - Fait: helper `app_format` pour dates/nombres/devise du profil; remplacement des formats codés en dur dans les UIs actives; libellés statistiques localisés.
- [x] 11. Ajouter des tests sur les zones critiques.
  - Fait: tests unitaires sur le modèle `Transaction` existant et nouveaux cas parser Wave/Orange Money SMS, y compris rejet sans marqueur OFMS.
- [x] 12. Supprimer ou reintegrer le widget mort `AddPlanning`.
  - Fait: suppression de `lib/widgets/add_planning.dart`, qui n'etait plus importe ni utilise.
- [x] 13. Ajouter l'idempotence SMS/notifications Wave et Orange Money.
  - Fait: clés d'idempotence persistantes par utilisateur, déduplication exact/ref/transfert opposé, et tests sur le cas dépôt+retrait du même mouvement.
- [x] 14. Debloquer le build Android production avec les plugins existants.
  - Fait: fallback `namespace` pour les vieux plugins Android, flavors Gradle alignes avec les commandes `make`, et build APK production valide.
- [x] 15. Corriger l'ecran noir apres suppression d'une transaction.
  - Fait: navigation de suppression stabilisee pour eviter les doubles `pop` apres confirmation.
- [x] 16. Remplacer l'ancien logo dans l'app et les icones.
  - Fait: `assets/images/new_logo.png` utilise dans les ecrans principaux et generation des launcher icons Android/iOS/web/desktop.
- [x] 17. Ajouter l'assistant IA finances avec prompt system.
  - Fait: ecran `Assistant finances`, contexte local transactions/budgets/todos, appel Groq Responses API et prompt system limitant les reponses aux donnees fournies.
- [x] 18. Injecter les acces API au build/runtime au lieu de les coder en dur.
  - Fait: `GROQ_API_KEY`, `GROQ_MODEL`, `SUPABASE_URL`, `SUPABASE_ANON_KEY` et `SUPABASE_PUBLISHABLE_KEY` passent par `--dart-define`/variables `make`; les vraies valeurs Supabase/Groq ne sont plus dans le code.
- [x] 19. Fiabiliser le lancement local Windows avec `.env.ps1`.
  - Fait: ajout de `make run-dev-local`, qui charge `.env.ps1` dans le meme processus PowerShell que `flutter run` et evite de recompiler avec des placeholders.
- [x] 20. Aligner le package Android debug avec la configuration Google Sign-In.
  - Fait: suppression du suffixe `.dev` sur le flavor `development`; le debug utilise maintenant `sn.alioukun.spendwise` comme la production.
- [x] 21. Corriger les reponses IA qui affichaient le raisonnement interne.
  - Fait: parsing Groq Responses limite aux blocs `message`/`output_text`, prompt renforce "reponse finale uniquement", rendu Markdown via `gpt_markdown`, et test anti-regression sur les blocs `reasoning`.
